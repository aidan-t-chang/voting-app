name: Menu Scraper

on:
  schedule:
    - cron: '0 7 * * *' # 1 AM CST
  workflow_dispatch:


jobs:
  trigger-api:
    runs-on: ubuntu-latest
    steps:
      - name: Wakey Wakey API
        run: curl --fail -X GET https://voting-app-api-j9fn.onrender.com/menu